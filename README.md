# use template in future projects
to use this template in new project, use this code:
```
git clone http://github.com/Arik59/react-template new-project
cd new-project
del -rf .git #delete old git connection
git init
```
* `new-project` - project name
